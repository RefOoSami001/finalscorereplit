{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "engines": {
    "node": ">=22 <25"
  },
  "packageManager": "pnpm@10.26.1",
  "scripts": {
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:prod": "pnpm run typecheck:libs && set BASE_PATH=/ && pnpm --filter @workspace/finalscore run build && pnpm --filter @workspace/api-server run build",
    "start": "NODE_ENV=production pnpm --filter @workspace/api-server run start",
    "start:prod": "NODE_ENV=production PORT=8000 pnpm --filter @workspace/api-server run start",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
